# Overrides the image default. Site config lives in sites-enabled/bookingsystem.conf.
# Listen on an unused port so the stock entrypoint cannot open :80 twice.
server {
    listen 127.0.0.1:8089;
    return 444;
}
